---
- name: initiate build
  hosts: localhost
  roles:
    - common

- name: clone sample images
  hosts: localhost
  roles:
    - clone-sample-images

- name: prepare custom images
  hosts: localhost
  roles:
    - prepare-custom-images

- name: prepare osbuild
  hosts: localhost
  roles:
    - prepare-osbuild

- name: run make
  hosts: localhost
  roles:
    - run-make
